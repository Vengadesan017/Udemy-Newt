# Performance
1. Create indexes
   - right indexes on db
   - execution plan
2. caching
   - first level caching
   - second level caching
   - distributed caching
   - carefull on size of first level caching
3. Eager and Lazy Fetching
   - use lazy mostly
   - eager is default to ToOne mapping
4. Avoiding n+1
   - when read the data in for loop
   - use lazy fetch
   - use entity graph and named entity graphs and dynamic entity graphs
   - use join fetch clause to reduce the db read

# Good pratice
1. make all varible as private inside the class

# Terminology
1. tight coupling - class (strong dependencies between components)
  - create class and call the class by obj
    - if yot wil to try to change the logic then it is hard
2. loose coupling  - interface
  - create interface and abstract method
  - change the child class we you need to cha ge the logic
3. application contenxt
4. dependency injection - inversionn of control
  - ioc container
    - types ApplicationCOntext - complex
    - BeanFactory - rare used simple features
5. spring beans
6. auto wires
7. @Component - class is managed by spring it will manage the obj by autowireing
8. dependency injection
   - types
     - Constructor based
     - setter based
     - field based - using refletion
# Spring models
1. core - IOC
2. Testing - mock obj, spring mvc test1
3. data access - transaction , JDBA , JPA
4. Web servlet - spring MVC
5. wen Reactive - Spring webFLux
