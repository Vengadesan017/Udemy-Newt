# Sub queries
- select * from dept where deptno < ( select deptno from dept where deptno = 30)
- select * from ( select * from dept)
- select job, ename, (select 'Hello there' from dual) from emp
# Joins
- select * from emp, dept where emp.deptno = dept.deptno
- select e.ename as first_name, job, sal, e.deptno from (select * from emp) e, (SELECT * from dept) d where e.deptno = d.deptno
- --
- select * from emp INNER join dept on emp.deptno = dept.deptno
- select * from emp left join dept on emp.deptno = dept.deptno
- select * from emp right join dept on emp.deptno = dept.deptno
- select * from emp right outer join dept on emp.deptno = dept.deptno
- select count(*) from emp, dept where emp.deptno = dept.deptno(+)
- select count(*) from emp, dept where emp.deptno(+) = dept.deptno --left outer join
- select * from emp full join dept on emp.deptno = dept.deptno
- - self
- select e.ename , m.ename manager from emp e , emp m where e.mgr = m.empno
- - natural
- select * from emp , dep
- -
- SELECT *  from emp WHERE EXISTS (select 'haii' from dual)  if exist it anything run the main query
- SELECT *  from emp WHERE NOT EXISTS (select 'haii' from dual)  if not exist anythig it run the main query
-  - Equi join
   ```sql
      CREATE TABLE job_grade
         (Grade_level varchar(2) not null,
          lowest_sal number not null,
          highest_sal number not null);
      
      
      INSERT ALL
          INTO job_grade
          VALUES ('A', 0, 1000)
          INTO job_grade
          VALUES ('B', 1001, 2000)
          INTO job_grade
          VALUES ('C', 2001, 3000)
          INTO job_grade
          VALUES ('D', 3001, 4000)
          INTO job_grade
          VALUES ('E', 4001, 5000)
      SELECT * FROM DUAL;
   ```
