# Relationship between tables
## One to one
- in models
  - use @OneToOne notation to refernece like private Person person
- in repo
  - in save()
    - Person p1 = new Person("rom")
    - em.persist(p1)
    - Pref r1 = new Pref("..")
    - r1.setPerson(p1)
    - em.persist(r1)
  - in viewPerson()
    - Pref pf1 = em.find(Pref.class,1)
    - sout pf1
    - sout pf1.getName()
- Lazy Fetch and @transactional
  - pf1 return only the pf1 values when yoy mention @OneToOne(fetch=FetchType.LAZY)
    - to get the person value use @Transactional in repo
      - it rollback when any error in repo
        - it maintain the persistenceContext through the block when you use @transactionl
        - if you not use @Transactional then you donot get the person value and update so because the persistence is closed
- Bidirectional
  - when you refer the table in both the table it create a duplicate of data like @onetooen in both model
  - use mappedBy="person" in @onetoone notation to remove this in physical db
    - but with this we will navigate the from bottom to top but reference in not in botttom table in physial but in obj
