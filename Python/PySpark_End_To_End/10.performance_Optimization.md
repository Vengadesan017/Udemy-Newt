# Performance & Optimization
1. Caching Data in Memory
2. Configuration Options
3. Join Strategy Hints for SQL Queries
4. Coalesce Hint for SQL Queries
5. Adaptive Query Execution
## joining Strategy
- steps
  - broadcast the smallest dataset to all executors
    - perform join by first broadcasting the smaller one to all spark executor using peer to peer protocal
  - perform a hash join
    - Create hash table based on the join key of smaller dataset
    - Loop over large dataset and match the hashed join key with the hash table created above
- types
  - shuffle hash join
  - shuffle sort merge join
  - cartesian product join
  - broadcast nested loop join
## Driver configuration
- When we apply collect(), take() operations on datasets, it requires the data to be moved to Driver. If we do so on huge dataset, it can crash the driver process with Out Of Memory errors(OOM)
- types
  - executor configurations
  - Parallelism configurations
## Memory management
- manage the memory in resource manager, node manager, application manager
- memory
  - off-heap memory
  - overhead memory
  - heap
- methods
  - dynamic occupancy mechanism
    - manage the memory of storage memory , execution memory, disc
