# JDBC to JPA
### H2 console
- spring.h2.console.enabled=true in properties
- then open /h2-console url
- create table in h2 consolde in browser and also insert the data
- 
### JDBC
- JDBC
  - work with create session ,open session, ps , run query , close with the help of serlevt 
- Spring JDBC
  - work with repo model .. with the help of spring
  - Project
    - Create package
    - create repo
      - @Repository
      - Obj for jdbcTemplate with Auto wire
      - defien findall() to model person
        - return jobcTemplate.query("select * from person", new BeanPropertyRowMapper(Person.class))
        - use queryForObject instead for query to use where
        - use update instead for query to use delete update insert
        - or create your own rowmapper in repo with implementing the ROwMapper<Person>
          - override the maprpw function and call the setter for all the attribute of model
    - create model person
      - crearte class Person with attribute
      - create getter , setter , constructor
    - In main method
      - implements CommandLineRUnner Interface    // to see the result in console
        - override run method to call the repo.findall()
      - create obj of LOgger
      - create obj for repo with Autowired
        
### JPA
- Create Model
  - mention the the model class as @Entity and @Id and @GeneratedValue optional Table(name ="person") , @Column(name="name",nullable = false)
- create repo
  - @mention as Repository @Transactional
    - create obj for EntitiyManager with @PersistenceContext
    - create findById method with id para
      - return the entittymanager.find(Person.class, id)
      - for update and insert
        - return entitymanager.merge(person_obj)
      - for delete
        - return entitymanager.remove(person_obj)
- in main method
  - create obj for repo with autowire
    - call findById with repo obj
      
### findAll using JPQL Named Query
- in repo in findAll() method
  - create obj for namedQuery as entityManager.createNamedQuery("find_all_person") with TypedQuery<Person> type
  - and return nq.getResultList() in findAll() menthod
- in models
  - mection as @NamedQuery(name="find_all_person",query="select p from person p")
  - also use @NamedQueries(value = {@..(... query="..."), @..(... query="...")} )
  - also native are there
    - in repo
      - em.createNativeQuery("Select * from course", Courese.class);

### Application properties
```
-- Enabling H2 Console
spring.h2.console.enabled=true

spring.datasource.url=jdbc:h2:mem:testdb

--Turn Statistics on
spring.jpa.properties.hibernate.generate_statistics=true
logging.level.org.hibernate.stat=debug

-- Show all queries
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate.type=trace
spring.jpa.properties.hibernate.connection.isolation=2

-- Performance
spring.jpa.properties.hibernate.jdbc.batch_size=10

-- Second Level Cache - Ehcache

1. enable second level cache
spring.jpa.properties.hibernate.cache.use_second_level_cache=true

2. specify the caching framework - JCache
 https://northcoder.com/post/setting-up-hibernate-with-jcache-an/
spring.jpa.properties.hibernate.cache.region.factory_class=org.hibernate.cache.jcache.internal.JCacheRegionFactory

3. Only cache what I tell to cache.
spring.jpa.properties.jakarta.persistence.sharedCache.mode=ENABLE_SELECTIVE

logging.level.net.sf.ehcache=debug

4. What data to cache?
spring.jpa.defer-datasource-initialization=true
```
## Entity Manager in repo
- clear  // Clears the current persistence context
- flush  // Writes changes to the database
- detach  // Detaches the entity
- refresh  // Reloads the entity from the databaserfd
